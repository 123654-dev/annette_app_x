// Enthält alle Dateiformate, die von der App bei der Konvertierung unterstützt werden.
// Wird beim Speichern von Dateien verwendet.
// Dabei muss das FileFormat die Endung der Datei angeben, z.B. "pdf" oder "jpg".
enum FileFormat {
  PDF,
  PNG,
  JPG,
  JSON,
  HTML,
}
