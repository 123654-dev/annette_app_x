import 'package:flutter/material.dart';

class TimetableWeekAll extends StatelessWidget {
  const TimetableWeekAll({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Timetable Week All');
  }
}